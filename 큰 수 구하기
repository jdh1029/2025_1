#include <stdio.h>
 
int main()
{
    int num[5];
    int i, max=0;
 
 
    for (i=0; i<5; i++) {
        printf ("숫자를 입력하세요 : ");
        scanf ("%d", &num[i]);
    }
 
    max = num[0];
 
    for (i=1; i<5; i++) {
 
            if (max < num[i])
                max = num[i];
    }
 
    printf ("가장 큰 수는 : %d", max);
 
    return 0;
 }
[출처] C언어 소스, 배열, 정수 5개를 입력받아서 가장 큰 수를 구하는 프로그램(최대값)|작성자 생활서비스

